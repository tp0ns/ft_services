apiVersion: v1
data: 
  config: |-
      address-pools:
      - name: default
        protocol: layer2
        addresses:
        - MINIKUBE-IP-MINIKUBE-IP
kind: ConfigMap
metadata: 
  name: config
  namespace: metallb-system